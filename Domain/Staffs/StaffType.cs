namespace DDDNetCore.Domain.Staffs
{
    /**
     * Represents the various types that a staff member can have.
     * Each type corresponds to a specific role within the healthcare system.
     */
    public enum StaffType
    {
        Doctor,
        Nurse,
        Other
    }
}